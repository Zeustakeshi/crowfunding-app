import React from "react";

const ErrorComponent = () => {
    return (
        <div className="bg-red-50 text-error font-semibold  rounded-lg p-3">
            This component is error !
        </div>
    );
};

export default ErrorComponent;
